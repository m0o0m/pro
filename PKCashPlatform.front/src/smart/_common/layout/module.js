(function(){
    "use strict";

    angular.module('SmartAdmin.Layout',  [])
        /*
        .run(function ($http, APP_CONFIG, $rootScope) {
            $http.get(APP_CONFIG.apiRootUrl + "/menu-items.json").then(function (res) {
                $rootScope.smartMenuItems = res;
                console.info('hello---1212');
            })
        });*/
})();